import React, { FC } from 'react';
import LoansManager from 'composites/loans-section/components/LoansManager';

const LoansPage: FC = () => {
    return <LoansManager />;
};

export default LoansPage;
